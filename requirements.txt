# Core
pandas>=2.2.0
Pillow>=10.0.0
python-dotenv>=1.0.0

# OpenAI VLM
openai>=1.40.0
# Pin pydantic <2.10 — gradio_client 1.3 (bundled with gradio 4.x) crashes on
# the newer schema output. Remove this pin once we move to gradio 5 (needs py3.10+).
pydantic>=2.6,<2.10

# Kaggle data download (optional, used by scripts/download_kaggle.py)
kaggle>=1.6.0

# Gradio PoC UI
# gradio 4.x requires the pre-1.0 huggingface_hub API (HfFolder); pin both
# until we migrate to gradio 5.x.
gradio>=4.40.0,<5.0
huggingface_hub<1.0

# TTS for accessibility playback
gTTS>=2.5.0
# pydub for concatenating gTTS segments with a silent breathing pause; the
# imageio-ffmpeg wheel ships an ffmpeg binary so users don't need a system one.
pydub>=0.25.0
imageio-ffmpeg>=0.5.0
